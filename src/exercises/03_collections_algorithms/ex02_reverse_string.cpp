#include <iostream>
#include <string>

int main() {
    std::string text;

    std::cout << "Enter text: ";
    std::getline(std::cin, text);

    // TODO: Reverse text manually (without std::reverse first)
    // TODO: Print reversed text
    // TODO (bonus): Palindrome check ignoring spaces/case

    return 0;
}
